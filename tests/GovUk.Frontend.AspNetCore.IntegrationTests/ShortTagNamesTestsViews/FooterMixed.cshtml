@model GovUk.Frontend.AspNetCore.IntegrationTests.ShortTagNamesTestsModel

@* The footer's own children all share the same parent element, so their spelling cannot be
   paired up through ParentTag; mixing the two must throw *@

<govuk-footer>
    <govuk-footer-content-licence />
    <copyright />
</govuk-footer>
