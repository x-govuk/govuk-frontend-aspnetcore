@model GovUk.Frontend.AspNetCore.IntegrationTests.ShortTagNamesTestsModel

@* The error summary's children all share the same parent element, so their spelling cannot be
   paired up through ParentTag; mixing the two must throw *@

<govuk-error-summary>
    <govuk-error-summary-title>There is a problem</govuk-error-summary-title>
    <error-summary-item href="#postcode-input">Enter a postcode, like AA1 1AA</error-summary-item>
</govuk-error-summary>
